node_modules/
dist/
portable/
.env
.omx/
.omc/
.venv-whisper/
data/

# AI
.claude/
.codex/
CLAUDE.md
AGENTS.md
.agestra

# Local planning docs are intentionally not committed.
docs/

references/*

npm-debug.log*

# Graphify knowledge graph (local-only, regenerated on demand)
graphify-out/
.graphifyignore
.graphify_python
# Graphify auto-rebuild git hooks (installed by `graphify hook install`, host-local — Python 경로 탐지 로직이 호스트별로 다름)
.githooks/post-checkout
.githooks/post-commit
